## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezone

  environment:
    WEBPY_ENV: test
    
test:
  override:
    - nosetests --with-xunit $CIRCLE_TEST_REPORTS/nosetests.xml
